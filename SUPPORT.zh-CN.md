# 支持

## 获取帮助

使用 GitHub Issues 反馈 bug、功能请求、文档不清晰问题和接入问题。

请包含 runtime 或使用方式、操作系统、安装路径，以及触发问题的准确 prompt。对于 runtime 专属问题，请说明使用的是 Codex、Claude Code 还是两者都有。

## 不支持范围

本仓库不提供托管评审机器人、模型服务或针对私有项目的策略咨询。

如果问题由 `review-fix-loop` 的 CLI 或 run record 行为引起，应在该项目中复现和反馈。
